{
  "version": 2,
  "builds": [
    {
      "src": "server.js", // Change this to your actual entry point
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.js" // Ensure this matches the entry point file
    }
  ]
}
